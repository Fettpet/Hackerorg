#ifndef COMPILEOPTIONS_H
#define COMPILEOPTIONS_H
/**
    if this value is set to 1, there are many Asseration tests to debug
    in case 0 there are no asserts
*/
#define DEBUGASSERT 0

/**
   if this value is set to 1, there are
*/
#define DEBUGOUTPUT 0
/**
  if it is 0 -> the cnf is writen whits Numbers
  if it is 1 -> the cnf is writen whis Varnames
*/
#define DEBUGVAR 0

/**
  it it is 0 -> no comments in Dimacs
  if it is 1 -> some comments are added to dimacs
*/
#define COMMENTSDIMACS 0



#endif // COMPILEOPTIONS_H
