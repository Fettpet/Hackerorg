Variablen:
 für jede zeile, spalte, Wert einen (n^4)

Constrains:
  + Jede Zelle hat nur einen Wert
  + Der Nachfolger muss in der selben Zeile, Spalte sein
  + Der Nachfolger muss entweder die selbe Farbe, Form haben
  ? Jeder Wert kommt nur einmal for
  x11 => x12 x13 x14 x15
  -x11 v x